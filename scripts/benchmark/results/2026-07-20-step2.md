# Step 2 on CPU, T4, and A100: 2026-07-20

The CPU changes remain the broadest Step 2 improvement, but the production
placement result is more favorable to CUDA than the original 16,000-variant
pilots suggested. For 700,000 variants and eight binary traits, score-only Step
2 takes 168.92 seconds on an eight-core N2, 27.29 seconds on one A100 process,
and 60.93 seconds on one T4 process. Balanced chromosome fan-out on the same
GPU reduces makespan to 13.61 seconds with four A100 processes and 54.77
seconds with two T4 processes while keeping the total host thread count fixed.

The GPUs were initially underfed. Score-only CUDA unnecessarily expanded every
packed genotype into a dense host matrix that the CUDA path never read.
Skipping that work reduces the A100 run from 37.17 to 27.29 seconds and the T4
run from 67.28 to 60.93 seconds with byte-identical output. Concurrent genomic
shards expose further overlap: A100 utilization rises from 25% to 49%, while
two T4 shards reach 90% utilization. The small remaining T4 gain indicates a
GPU throughput limit; the A100 still has CPU-pipeline headroom.

Correction-heavy work is different. With the same LOCO predictions and
`--pThresh 0.01`, binary approximate Firth takes 252.56 seconds on the N2 and
114.92 seconds on A100, but A100 utilization is only 6.5%. The GPU accelerates
the initial score, while correction remains on the CPU. A larger CPU machine
could therefore close much of that gap. CUDA should be retained as a narrow
score accelerator, not presented as a general Step 2 implementation.

## Workloads

All comparisons use the same physical PGEN fixture: 50,000 samples and 700,000
variants with real LD structure. Production Step 2 commonly covers 10-100
million variants; 700,000 variants is used here to observe hundreds of blocks
and steady GPU feeding, not as a production-scale elapsed-time claim. The
phenotype panels are:

- 32 quantitative traits, either complete or with per-trait missingness rising
  from 0% to 10%;
- eight binary traits with prevalences from 1% to 50%; and
- eight survival traits with observed-event fractions from 10% to 80%.

The N2 full runs use eight physical Xeon cores, eight REGENIE threads, and
oneMKL 2026.1. A100 runs use 12 total host threads and existing LOCO
predictions. T4 runs use four total host threads and `--ignore-pred`; this gives
T4 a small advantage in cross-system elapsed-time comparisons. The earlier
integrated CPU/CUDA pairs read the first 16,000 variants with a block size of
1,000 and use identical options within each machine.

The original integrated implementation is `94c5d98`; packed host-expansion
elision is `ae9144d`. Every binary links to oneMKL.

## Full eight-core CPU anchor

The baseline is `114ef81`; the retained CPU optimization is `db1ce23`.

| Model | Traits | Missingness or correction | Baseline wall | Optimized wall | Throughput |
| --- | ---: | --- | ---: | ---: | ---: |
| Quantitative | 32 | 0-10% by trait | 783.62 s | 263.47 s | 2.97x |
| Binary | 8 | approximate Firth | 307.40 s | 252.56 s | 1.22x |
| Survival | 8 | approximate Firth | 433.29 s | 313.80 s | 1.38x |

The quantitative run cuts genotype work from 415.53 to 93.14 seconds and
variant compute from 345.13 to 147.54 seconds. Binary genotype work remains
22.79 seconds while compute falls from 274.28 to 219.61 seconds. Survival
genotype work remains 23.01 seconds while compute falls from 395.78 to 276.51
seconds. Binary and survival Step 2 are plainly not dominated by variant
reading at this trait count.

The CPU implementation avoids work in three places:

- Missing-phenotype accounting and quantitative scoring update only affected
  traits instead of repeatedly applying full sample-by-trait masks.
- Binary scoring uses cached projection crossproducts and materializes a
  sample-length residualized genotype only when a correction is selected.
- Default Cox scoring uses cached Gram and score terms. `--coxscore-exact`
  retains the original calculation, and a residualized genotype is deferred
  until a selected Firth correction needs it.

Complete-case quantitative timing is unchanged in the 16,000-variant CPU
control (5.59 seconds before, 5.55 seconds after). The other pilot results are
recorded in [`2026-07-20-step2.tsv`](2026-07-20-step2.tsv).

## Steady-state placement comparison

These runs process all 700,000 variants. The speedup column compares each
placement with the measured eight-core N2 score-only run. GPU utilization is
the mean from the first to the last nonzero 100 ms sample, including idle gaps
between kernels.

| Placement | Processes x threads | Wall | Variants/s | vs. N2 | GPU util. |
| --- | ---: | ---: | ---: | ---: | ---: |
| Eight-core N2 | 1 x 8 | 168.92 s | 4,144 | 1.00x | n/a |
| A100 before host-expansion elision | 1 x 12 | 37.17 s | 18,835 | 4.54x | 20.3% |
| A100 at `ae9144d` | 1 x 12 | 27.29 s | 25,652 | 6.19x | 25.3% |
| A100 at `ae9144d`, balanced fan-out | 4 x 3 | 13.61 s | 51,433 | 12.41x | 48.8% |
| T4 before host-expansion elision | 1 x 4 | 67.28 s | 10,404 | 2.51x | 49.3% |
| T4 at `ae9144d` | 1 x 4 | 60.93 s | 11,488 | 2.77x | 64.8% |
| T4 at `ae9144d`, balanced fan-out | 2 x 2 | 54.77 s | 12,781 | 3.08x | 90.5% |

On A100, packed-host expansion accounts for most PGEN ingestion time: that
phase falls from 9.27 to 2.31 seconds. The corresponding T4 phase falls from
15.61 to 4.47 seconds. All eight full A100 and T4 output files are
byte-for-byte identical before and after the change. On A100, the
16,000-variant quantitative, binary, and survival validations are also
byte-identical across all 48 files; the T4 binary pilot matches across all
eight files.

Fan-out is an experiment in overlap, not a recommendation to multiply GPU
processes without limit. The four A100 processes divide the chromosomes into
near-equal variant counts and retain 12 total host threads. Their combined CUDA
kernel time is 6.68 seconds, essentially the same work as the single process,
but CPU preparation from one shard overlaps GPU work from another. The T4
reaches saturation with two processes: utilization rises from 64.8% to 90.5%,
yet makespan improves by only 10%. This argues against expecting an enormous
additional T4 gain from transfer or scheduling work alone.

The matched correction result is:

| Placement | Prediction mode | Corrected tests | Wall | vs. N2 | GPU util. |
| --- | --- | ---: | ---: | ---: | ---: |
| Eight-core N2 | LOCO | 59,904 | 252.56 s | 1.00x | n/a |
| A100 | LOCO | 59,904 | 114.92 s | 2.20x | 6.5% |
| T4 | ignored | 57,990 | 219.41 s | 1.15x | 25.7% |

All three use approximate Firth with `--pThresh 0.01`. The A100 and N2
correction counts match. T4 omits predictions, which changes its scores and
selects fewer corrections, so its row is slightly advantaged and not a strict
statistical match. Low utilization is expected here because the correction
path is still CPU-only.

## Integrated CUDA result

These are end-to-end elapsed times, including PGEN handling, null-model setup,
selective corrections, and result output. A speedup greater than 1 means CUDA
reduced wall time. This table records the original 16,000-variant `94c5d98`
integration before packed host-expansion elision. Its Firth rows use
`--pThresh 0.05` and are within-machine engineering controls; use the full-run
table above for the matched `0.01` placement comparison.

| System | Workload | CPU wall | CUDA wall | Speedup |
| --- | --- | ---: | ---: | ---: |
| T4, four CPU threads | Quantitative, 32 complete traits | 6.48 s | 7.29 s | 0.89x |
| T4, four CPU threads | Quantitative, 32 traits, 0-10% missing | 15.35 s | 30.60 s | 0.50x |
| T4, four CPU threads | Binary, eight traits, score only | 10.81 s | 4.48 s | 2.41x |
| T4, four CPU threads | Binary, eight traits, approximate Firth | 25.34 s | 18.32 s | 1.38x |
| T4, four CPU threads | Survival, eight traits, score only | 14.18 s | 10.88 s | 1.30x |
| T4, four CPU threads | Survival, eight traits, approximate Firth | 26.90 s | 21.68 s | 1.24x |
| A100, 12 CPU threads | Quantitative, 32 complete traits | 5.87 s | 5.82 s | 1.01x |
| A100, 12 CPU threads | Quantitative, 32 traits, 0-10% missing | 9.97 s | 8.83 s | 1.13x |
| A100, 12 CPU threads | Binary, eight traits, score only | 6.82 s | 3.77 s | 1.81x |
| A100, 12 CPU threads | Binary, eight traits, approximate Firth | 15.76 s | 11.97 s | 1.32x |
| A100, 12 CPU threads | Survival, eight traits, score only | 8.32 s | 4.51 s | 1.84x |
| A100, 12 CPU threads | Survival, eight traits, approximate Firth | 16.52 s | 11.37 s | 1.45x |

The GPU receives packed PGEN hardcalls directly. Phenotype residuals,
missingness masks, weights, and null-model projection terms stay resident for
the chromosome; only per-block hardcalls go up and small score numerator and
denominator matrices come back. Filtering, result formatting, SPA/Firth, and
exact Cox scoring remain on the CPU. At `ae9144d`, score-only workflows also
skip host-side Eigen genotype materialization that had become dead work.

The current CUDA path is deliberately narrow: additive single-variant hardcall
tests from PGEN, separate phenotype outputs, all analyzed samples present in
the genotype input, and at most 16 covariates. Unsupported modes use the CPU
implementation. Non-PAR blocks that require male dosage rescaling also remain
on the CPU.

## What the GPU telemetry says

| System | Workload | CUDA kernel | Whole-run GPU util. | Scoring-interval util. | Scoring-interval power |
| --- | --- | ---: | ---: | ---: | ---: |
| T4 | Quantitative, complete | 3.141 s | 43.3% | 58.3% | 54.8 W / 70 W |
| T4 | Quantitative, 0-10% missing | 22.831 s | 75.8% | 83.0% | 64.0 W / 70 W |
| T4 | Binary, score only | 0.895 s | 19.9% | 29.7% | 50.7 W / 70 W |
| T4 | Binary, Firth | 1.384 s | 8.1% | 9.2% | 33.9 W / 70 W |
| T4 | Survival, score only | 6.750 s | 61.8% | 78.3% | 60.7 W / 70 W |
| T4 | Survival, Firth | 6.754 s | 32.5% | 37.1% | 51.7 W / 70 W |
| A100 | Quantitative, complete | 0.489 s | 8.3% | 14.3% | 81.2 W / 400 W |
| A100 | Quantitative, 0-10% missing | 1.298 s | 13.2% | 17.8% | 90.2 W / 400 W |
| A100 | Binary, score only | 0.154 s | 4.5% | sampling-limited | 52.3 W / 400 W |
| A100 | Survival, score only | 0.302 s | 7.8% | sampling-limited | 54.8 W / 400 W |

Telemetry was sampled every 250 ms. That is too coarse to characterize the
subsecond A100 binary and survival kernels. The 100 ms, 700,000-variant runs
above supersede it for steady-state utilization. The T4 missing-quantitative
kernel is genuinely busy and still loses badly. Occupancy and power are
diagnostic signals, not acceptance criteria.

Firth makes the distinction especially clear. CUDA still accelerates the
initial score pass, but thousands of selected corrections execute on the CPU.
That is why utilization falls in both the pilot and full Firth runs. Moving the
score calculation alone cannot remove that correction cost.

## Numerical checks

For the retained CPU optimization, every 16,000-variant pilot is byte-for-byte
identical to `114ef81`, as is the full binary Firth run. In the full
quantitative run, one of 22.4 million rows changes the last printed digit. In
the full survival run, one of 5.6 million rows prints one numeric field at
slightly different precision; beta, standard error, and p-value are unchanged.
The exact Cox control is
byte-for-byte identical.

CPU and CUDA generated identical files for both T4 quantitative panels, the
A100 missing-quantitative panel, all A100 binary and survival panels, and both
T4 survival panels. Two reductions differ only at the output precision limit:

- A100 complete quantitative: one standard error in 512,000 output rows ends
  in `...90` on CPU and `...91` on CUDA; beta, chi-square, and p-value match.
- T4 binary: one exactly null score in 128,000 rows is approximately
  `1.3e-15` on CPU and `1.0e-15` on CUDA. The same row appears in score-only
  and Firth output, but it is not selected for correction.

All other files in those pairs are byte-for-byte identical. No Firth test
failed. The final source builds and passes all three available CTest targets on
the eight-core CPU, T4, and A100 configurations.

## Default behavior and practical conclusion

Removing Step 2 CUDA is not justified by these results. The score-only binary
path is 6.19x faster than the measured eight-core N2 in one A100 process and
2.77x faster in one T4 process. Balanced fan-out raises those placement ratios
to 12.41x and 3.08x. The 700,000-variant fixture is smaller than production,
but fixed setup is already only 4.9% of the N2 run; at 10-100 million variants,
steady per-variant throughput matters even more.

The feature should remain explicitly narrow. It is a packed PGEN score
accelerator, not a general GPU Step 2 engine. CPU fan-out remains the simplest
broad production architecture, particularly for quantitative missingness,
exact Cox, and correction-heavy analyses. A larger N2 benchmark is still
needed for a cost- and placement-quality decision: the current comparison has
only eight physical CPU cores, and Firth's low GPU utilization makes its A100
advantage especially likely to shrink on a larger CPU machine.

There is more A100 work worth doing. Four concurrent shards double throughput
relative to the optimized single process while reaching only 49% mean GPU
utilization, demonstrating that an internal multi-block CPU/GPU pipeline could
improve a one-process workflow. T4 is different: two shards reach 90% mean
utilization for only a further 10% wall-time gain. Major additional T4 binary
improvement would require a faster kernel or a different statistical workload,
not simply better feeding.

In CUDA builds, `--compute-backend auto` makes a per-workflow choice. It keeps
quantitative Step 2 on the CPU for pre-Ampere GPUs while allowing supported
binary and survival scores to use CUDA. Explicit `--compute-backend cuda`
remains useful for measurement and controlled deployment, and
`--step2-profile` reports the backend that actually scored the variants.

The earlier standalone packed-input probe is retained in
[`2026-07-20-step2-cuda.tsv`](2026-07-20-step2-cuda.tsv). It established that
the arithmetic could run on both GPUs, but it substantially overpredicted T4
quantitative and Cox performance because it did not reproduce the integrated
missingness and null-model work. It should be treated as a kernel diagnostic,
not an elapsed-time forecast. The integrated measurements are in
[`2026-07-20-step2-integrated.tsv`](2026-07-20-step2-integrated.tsv). The full
placement, utilization, and fan-out measurements are in
[`2026-07-20-step2-steady-state.tsv`](2026-07-20-step2-steady-state.tsv).
